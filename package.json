{
  "name": "chip8",
  "version": "1.0.0",
  "main": "index.js",
  "author": "strothj <jason@jasons.io>",
  "license": "MIT",
  "scripts": {
    "start": "yarn clean && tsc -b --watch",
    "build": "yarn clean && tsc -b",
    "clean": "rimraf dist/js"
  },
  "devDependencies": {
    "cz-conventional-changelog": "3.1.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
